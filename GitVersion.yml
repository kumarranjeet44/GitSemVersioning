versioningMode: ContinuousDelivery
branches:
  master:
    regex: ^master$
    tag: 'v'
    increment: Minor
  develop:
    regex: ^develop$
    tag: 'dev'
    increment: Minor
  feature:
    regex: ^features?[/-]
    tag: 'feat'
    increment: Patch
