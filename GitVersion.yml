versioningMode: ContinuousDelivery
branches:
  master:
    regex: ^master$
    tag: 'v'
    increment: Minor
    prevent-increment-of-merged-branch-version: true
  develop:
    regex: ^develop$
    tag: 'dev'
    increment: Minor
